<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @course.name %>
</p>

<p>
  <strong>gym:</strong>
  <%= @course.gym %>
</p>

<p>
  <strong>Likes:</strong>
  <%= @course.likes %>
</p>


  <strong>comments:</strong>
<tbody>
  <% @course.course_comments.each do |comment| %>
    <tr>
      <td><%= comment.text %></td>
    </tr>
  <% end %>
</tbody>
</br>

<% if athlete_signed_in? %>
  <td><%= link_to "Take a Comment", new_course_comment_path(course: @course.id)%></td>
<% end %></br></br>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
