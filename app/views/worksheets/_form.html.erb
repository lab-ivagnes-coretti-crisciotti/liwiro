<%= form_with(model: worksheet, local: true) do |form| %>
  <% if worksheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(worksheet.errors.count, "error") %> prohibited this worksheet from being saved:</h2>

      <ul>
      <% worksheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="card">
        <div class="card-body">
          <div class="field">

            <div class="container">
              <div class="row">
                <div class="col">
                  <% if gym_signed_in? %>

                  <div class="card" style="width: 18rem;">
                    <div class="card-header">
                      <%= form.label :exercise %>
                    </div>
                    <ul class="list-group list-group-flush">
                      <li class="list-group-item">
                        <%= form.check_box :ex1, {}, "ex1" %>
                        <%= "ex1".humanize %>
                      </li>
                      <li class="list-group-item">
                        <%= form.check_box :ex2, {}, "ex2" %>
                        <%= "ex2".humanize %>
                      </li>
                      <li class="list-group-item">
                        <%= form.check_box :ex3, {}, "ex3" %>
                        <%= "ex3".humanize %>
                      </li>
                    </ul>
                  </div>

                </div>
                <div class="col">

                  <%= form.label :comments %>
                  <%= form.text_field :comments , :class=>"form-control"%>

                  <%= form.hidden_field :completed, value: true %>
              <% end %>

                  <% if athlete_signed_in? %>
                    <%= form.hidden_field :gym_id, value: params[:gym] %>
                  <% end %>

                </div>
              </div>
            </div>
            <br>

            <div class="actions">
              <%= form.submit :class =>"btn btn-primary btn-block" %>
            </div>
            <br>
          <% end %>

          </div>
        </div>
      </div>
      <br>
